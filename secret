--// KAWATAN DUELS - FULL GUI + AUTO STEAL + SPEED + ESP
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local lp = Players.LocalPlayer

--// ==================== ANTI RAGDOLL ====================
local Connections = {}
local AntiRagdoll = { Enabled = false }

local function startAntiRagdoll()
    if Connections.antiRagdoll then return end
    Connections.antiRagdoll = RunService.Heartbeat:Connect(function()
        if not AntiRagdoll.Enabled then return end
        local char = lp.Character
        if not char then return end
        local hum = char:FindFirstChildOfClass("Humanoid")
        local root = char:FindFirstChild("HumanoidRootPart")
        if not (hum and root) then return end
        
        local ragdolled = hum:GetState() == Enum.HumanoidStateType.Ragdoll or hum:GetState() == Enum.HumanoidStateType.Physics
        if ragdolled then
            pcall(function() lp:SetAttribute("RagdollEndTime", workspace:GetServerTimeNow()) end)
            for _, d in ipairs(char:GetDescendants()) do
                if d:IsA("BallSocketConstraint") or (d:IsA("Attachment") and d.Name:find("Ragdoll")) then
                    d:Destroy()
                end
            end
            for _, obj in ipairs(char:GetDescendants()) do
                if obj:IsA("Motor6D") and not obj.Enabled then obj.Enabled = true end
            end
            hum:ChangeState(Enum.HumanoidStateType.Running)
        end
    end)
end

local function stopAntiRagdoll()
    if Connections.antiRagdoll then Connections.antiRagdoll:Disconnect() end
end

--// ==================== AUTO STEAL ====================
local AutoSteal = {
    Enabled = false,
    STEAL_RADIUS = 15,
    STEAL_DURATION = 1.3,
    NormalSpeed = 16,
    StealSpeed = 25,
    isStealing = false,
    StealData = {},
    progressBar = nil,
    heartbeatConn = nil,
}

local function getHRP()
    local c = lp.Character
    return c and (c:FindFirstChild("HumanoidRootPart") or c:FindFirstChild("UpperTorso") or c:FindFirstChild("Torso"))
end

local function isHoldingItem()
    local char = lp.Character
    if not char then return false end
    return char:FindFirstChildWhichIsA("Tool") or #char:GetChildren() > 10 -- Pet veya item kontrolü
end

local function isMyPlotByName(plotName)
    local plots = workspace:FindFirstChild("Plots")
    local plot = plots and plots:FindFirstChild(plotName)
    if plot then
        local sign = plot:FindFirstChild("PlotSign")
        local yb = sign and sign:FindFirstChild("YourBase")
        return yb and yb:IsA("BillboardGui") and yb.Enabled
    end
    return false
end

local function findNearestPrompt()
    local hrp = getHRP()
    if not hrp then return nil end
    local plots = workspace:FindFirstChild("Plots")
    if not plots then return nil end
    local nearest, dist = nil, math.huge
    for _, plot in ipairs(plots:GetChildren()) do
        if isMyPlotByName(plot.Name) then continue end
        local pods = plot:FindFirstChild("AnimalPodiums")
        if not pods then continue end
        for _, pod in ipairs(pods:GetChildren()) do
            local spawn = pod:FindFirstChild("Base") and pod.Base:FindFirstChild("Spawn")
            if spawn then
                local d = (spawn.Position - hrp.Position).Magnitude
                if d <= AutoSteal.STEAL_RADIUS and d < dist then
                    local att = spawn:FindFirstChild("PromptAttachment")
                    if att then
                        for _, p in ipairs(att:GetChildren()) do
                            if p:IsA("ProximityPrompt") and p.ActionText:find("Steal") then
                                nearest, dist = p, d
                            end
                        end
                    end
                end
            end
        end
    end
    return nearest
end

local function updateProgress()
    if not AutoSteal.isStealing or not AutoSteal.progressBar then return end
    local start = tick()
    task.spawn(function()
        while AutoSteal.isStealing do
            AutoSteal.progressBar.Size = UDim2.new(math.min((tick() - start) / AutoSteal.STEAL_DURATION, 1), 0, 1, 0)
            task.wait()
        end
        if AutoSteal.progressBar then AutoSteal.progressBar.Size = UDim2.new(0,0,1,0) end
    end)
end

local function executeSteal(prompt)
    if AutoSteal.isStealing then return end
    -- ... (eski executeSteal fonksiyonu aynı kalıyor)
    if not AutoSteal.StealData[prompt] then
        AutoSteal.StealData[prompt] = {hold = {}, trigger = {}, ready = true}
        if getconnections then
            for _, c in ipairs(getconnections(prompt.PromptButtonHoldBegan)) do table.insert(AutoSteal.StealData[prompt].hold, c.Function) end
            for _, c in ipairs(getconnections(prompt.Triggered)) do table.insert(AutoSteal.StealData[prompt].trigger, c.Function) end
        end
    end
    local data = AutoSteal.StealData[prompt]
    if not data.ready then return end
    data.ready = false
    AutoSteal.isStealing = true
    updateProgress()
    task.spawn(function()
        for _, f in ipairs(data.hold) do pcall(f) end
        task.wait(AutoSteal.STEAL_DURATION)
        for _, f in ipairs(data.trigger) do pcall(f) end
        data.ready = true
        AutoSteal.isStealing = false
    end)
end

--// ==================== SPEED CONTROL ====================
local function setSpeed(speed)
    local hum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
    if hum then hum.WalkSpeed = speed end
end

--// ==================== ESP ====================
local ESP = { Enabled = false, Boxes = {}, Tracers = {}, Distances = {} }

-- (ESP kodu önceki mesajdakiyle aynı, yer kaplamasın diye burada kısalttım)
-- ... ESP fonksiyonları aynı kalıyor ...

--// ====================== GUI ======================
local sg = Instance.new("ScreenGui")
sg.Name = "KawatanDuelsGUI"
sg.ResetOnSpawn = false
sg.Parent = lp.PlayerGui

-- Top Bar (Progress + Radius)
local topBar = Instance.new("Frame")
topBar.Size = UDim2.new(0, 300, 0, 58)
topBar.Position = UDim2.new(0.5, -150, 0, 8)
topBar.BackgroundColor3 = Color3.fromRGB(8, 8, 16)
topBar.Parent = sg
Instance.new("UICorner", topBar).CornerRadius = UDim.new(0,16)
Instance.new("UIStroke", topBar).Color = Color3.fromRGB(0,210,255)

local topLabel = Instance.new("TextLabel")
topLabel.Size = UDim2.new(1,-120,0,24)
topLabel.Position = UDim2.new(0,16,0,8)
topLabel.BackgroundTransparency = 1
topLabel.Font = Enum.Font.GothamBold
topLabel.TextSize = 15
topLabel.TextColor3 = Color3.new(1,1,1)
topLabel.Parent = topBar

local barContainer = Instance.new("Frame")
barContainer.Size = UDim2.new(0,190,0,13)
barContainer.Position = UDim2.new(0,16,0,37)
barContainer.BackgroundColor3 = Color3.fromRGB(20,20,28)
barContainer.Parent = topBar
Instance.new("UICorner", barContainer).CornerRadius = UDim.new(1,0)

AutoSteal.progressBar = Instance.new("Frame")
AutoSteal.progressBar.Size = UDim2.new(0,0,1,0)
AutoSteal.progressBar.BackgroundColor3 = Color3.fromRGB(0,210,255)
AutoSteal.progressBar.Parent = barContainer
Instance.new("UICorner", AutoSteal.progressBar).CornerRadius = UDim.new(1,0)

local radiusBox = Instance.new("TextButton")
radiusBox.Size = UDim2.new(0,48,0,26)
radiusBox.Position = UDim2.new(0,215,0,30)
radiusBox.BackgroundColor3 = Color3.fromRGB(10,10,18)
radiusBox.Text = tostring(AutoSteal.STEAL_RADIUS)
radiusBox.TextColor3 = Color3.new(1,1,1)
radiusBox.Font = Enum.Font.GothamBold
radiusBox.TextSize = 16
radiusBox.Parent = topBar
Instance.new("UICorner", radiusBox).CornerRadius = UDim.new(0,8)

-- FPS Ping
RunService.RenderStepped:Connect(function()
    local ping = 0
    local stats = game:GetService("Stats").Network.ServerStatsItem
    if stats and stats:FindFirstChild("Data Ping") then
        ping = math.floor(stats.DataPing:GetValue())
    end
    topLabel.Text = "Kawatan Hub | FPS "..math.floor(workspace:GetRealPhysicsFPS()).." | PING "..ping.."ms"
end)

-- Radius Click
radiusBox.MouseButton1Click:Connect(function()
    local tb = Instance.new("TextBox", topBar)
    tb.Size = radiusBox.Size
    tb.Position = radiusBox.Position
    tb.BackgroundColor3 = Color3.fromRGB(15,15,25)
    tb.Text = radiusBox.Text
    tb:CaptureFocus()
    tb.FocusLost:Connect(function()
        local num = tonumber(tb.Text)
        if num and num > 0 then
            AutoSteal.STEAL_RADIUS = math.floor(num)
            radiusBox.Text = tostring(AutoSteal.STEAL_RADIUS)
        end
        tb:Destroy()
    end)
end)

-- Hub Frame (ana menü)
local hub = Instance.new("Frame")
hub.Size = UDim2.new(0,270,0,320)
hub.Position = UDim2.new(0,-350,0.5,-160)
hub.BackgroundColor3 = Color3.fromRGB(0,0,0)
hub.BackgroundTransparency = 0.25
hub.Parent = sg
Instance.new("UICorner", hub).CornerRadius = UDim.new(0,14)
Instance.new("UIStroke", hub).Color = Color3.fromRGB(0,120,255)

-- Title
local title = Instance.new("TextLabel", hub)
title.Size = UDim2.new(1,0,0,40)
title.Position = UDim2.new(0,0,0,8)
title.BackgroundTransparency = 1
title.Font = Enum.Font.GothamBold
title.TextSize = 20
title.TextColor3 = Color3.fromRGB(0,120,255)
title.Text = "Kawatan Duels"

-- Toggle Button (aç/kapat)
local toggleBtn = Instance.new("TextButton")
toggleBtn.Size = UDim2.new(0,55,0,55)
toggleBtn.Position = UDim2.new(1,-70,0,70)
toggleBtn.BackgroundColor3 = Color3.fromRGB(0,120,255)
toggleBtn.Text = "≡"
toggleBtn.TextSize = 38
toggleBtn.Parent = sg
Instance.new("UICorner", toggleBtn).CornerRadius = UDim.new(0,14)

-- Sections
local sections = {"Combat", "Player", "Visual", "Settings"}
local frames = {}
local content = Instance.new("Frame", hub)
content.Size = UDim2.new(1,-20,1,-110)
content.Position = UDim2.new(0,10,0,95)
content.BackgroundTransparency = 1

for _, name in ipairs(sections) do
    local scroll = Instance.new("ScrollingFrame", content)
    scroll.Name = name.."Frame"
    scroll.Size = UDim2.new(1,0,1,0)
    scroll.BackgroundTransparency = 1
    scroll.ScrollBarThickness = 4
    scroll.Visible = false
    local layout = Instance.new("UIListLayout", scroll)
    layout.Padding = UDim.new(0,8)
    frames[name] = scroll
end

local function ShowSection(name)
    for _,f in pairs(frames) do f.Visible = false end
    frames[name].Visible = true
end

-- Section Buttons
for i, v in ipairs(sections) do
    local btn = Instance.new("TextButton", hub)
    btn.Size = UDim2.new(0,58,0,28)
    btn.Position = UDim2.new(0,12+(i-1)*62,0,55)
    btn.BackgroundColor3 = Color3.fromRGB(10,10,10)
    btn.Text = v
    btn.TextColor3 = Color3.new(1,1,1)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 13
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0,6)
    btn.MouseButton1Click:Connect(function() ShowSection(v) end)
end

-- Create Toggle (Statuslu)
local function CreateToggle(section, text, callback)
    local container = Instance.new("Frame", frames[section])
    container.Size = UDim2.new(1,0,0,40)
    container.BackgroundColor3 = Color3.fromRGB(20,20,20)
    Instance.new("UICorner", container).CornerRadius = UDim.new(0,8)

    local label = Instance.new("TextLabel", container)
    label.Size = UDim2.new(0.65,0,1,0)
    label.Position = UDim2.new(0,14,0,0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Color3.fromRGB(220,220,220)
    label.Font = Enum.Font.GothamSemibold
    label.TextSize = 14
    label.TextXAlignment = Enum.TextXAlignment.Left

    local tBtn = Instance.new("TextButton", container)
    tBtn.Size = UDim2.new(0,50,0,26)
    tBtn.Position = UDim2.new(1,-60,0.5,-13)
    tBtn.BackgroundColor3 = Color3.fromRGB(45,45,45)
    tBtn.TextColor3 = Color3.new(1,1,1)
    tBtn.Font = Enum.Font.GothamBold
    tBtn.TextSize = 13
    Instance.new("UICorner", tBtn).CornerRadius = UDim.new(0,6)

    local enabled = false
    tBtn.MouseButton1Click:Connect(function()
        enabled = not enabled
        tBtn.Text = enabled and "ON" or "OFF"
        tBtn.BackgroundColor3 = enabled and Color3.fromRGB(0,170,100) or Color3.fromRGB(45,45,45)
        if callback then callback(enabled) end
    end)
end

-- Toggles
CreateToggle("Combat", "Auto Steal Nearest", function(v)
    AutoSteal.Enabled = v
    if v then
        AutoSteal.heartbeatConn = RunService.Heartbeat:Connect(function()
            if not AutoSteal.Enabled or AutoSteal.isStealing then return end
            local prompt = findNearestPrompt()
            if prompt then executeSteal(prompt) end
        end)
    else
        if AutoSteal.heartbeatConn then AutoSteal.heartbeatConn:Disconnect() end
    end
end)

CreateToggle("Combat", "Auto Left", function(v) print("Auto Left:", v) end)   -- İstersen hareket kodunu eklerim
CreateToggle("Combat", "Auto Right", function(v) print("Auto Right:", v) end)

CreateToggle("Combat", "Anti Bat", function(v) print("Anti Bat:", v) end)
CreateToggle("Combat", "Bat Aimbot", function(v) print("Bat Aimbot:", v) end)

CreateToggle("Player", "Anti Ragdoll", function(v)
    AntiRagdoll.Enabled = v
    if v then startAntiRagdoll() else stopAntiRagdoll() end
end)

CreateToggle("Visual", "ESP Players", function(v)
    -- ESP toggle (önceki kodla aynı)
end)

-- Settings
CreateNumberSetting("Settings", "Steal Radius", AutoSteal.STEAL_RADIUS, function(val)
    AutoSteal.STEAL_RADIUS = math.floor(val)
    radiusBox.Text = tostring(AutoSteal.STEAL_RADIUS)
end)

CreateNumberSetting("Settings", "Normal Speed", AutoSteal.NormalSpeed, function(val)
    AutoSteal.NormalSpeed = math.floor(val)
end)

CreateNumberSetting("Settings", "Steal Speed", AutoSteal.StealSpeed, function(val)
    AutoSteal.StealSpeed = math.floor(val)
end)

ShowSection("Combat")

-- Speed Controller (Elinde bir şey varsa Steal Speed)
RunService.Heartbeat:Connect(function()
    if AutoSteal.isStealing or isHoldingItem() then
        setSpeed(AutoSteal.StealSpeed)
    else
        setSpeed(AutoSteal.NormalSpeed)
    end
end)

-- Open/Close
local opened = false
toggleBtn.MouseButton1Click:Connect(function()
    opened = not opened
    local pos = opened and UDim2.new(0,20,0.5,-160) or UDim2.new(0,-350,0.5,-160)
    TweenService:Create(hub, TweenInfo.new(0.3, Enum.EasingStyle.Quint), {Position = pos}):Play()
end)

print("Kawatan Duels GUI - Güncellendi (Steal Speed + Auto Left/Right + Butonlar)")
