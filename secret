local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local ProximityPromptService = game:GetService("ProximityPromptService")
local CoreGui = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer

local CONFIG = {
    Gui = {
        ScreenGuiName = "Meowl Hub",
        ResetOnSpawn = false,
        ZIndexBehavior = Enum.ZIndexBehavior.Sibling,
    },
    MainFrame = {
        Name = "Main",
        Size = UDim2.new(0, 220, 0, 160),
        Position = UDim2.new(0.5, -110, 0.5, -80),
        BackgroundColor3 = Color3.fromRGB(8, 20, 12),
        BackgroundTransparency = 0.08,
        BorderSizePixel = 0,
        Active = true,
    },
    TitleBar = {
        Height = 28,
        Text = "Meowl Hub - Steal",
        TextColor3 = Color3.fromRGB(120, 255, 160),
        TextSize = 13,
    },
    StealSpeed = {
        Label = "Steal Speed",
        DefaultValue = 85,
        Min = 50,
        Max = 100,
        BarColor = Color3.fromRGB(25, 35, 25),
        FillColor = Color3.fromRGB(80, 255, 120),
        KnobColor = Color3.fromRGB(220, 255, 230),
    },
    Trigger = {
        Label = "Trigger Percent",
        DefaultPercent = 92,
        BarColor = Color3.fromRGB(25, 35, 25),
        FillColor = Color3.fromRGB(100, 200, 255),
        KnobColor = Color3.fromRGB(220, 255, 230),
    },
    Accent = {
        StrokeColor = Color3.fromRGB(80, 255, 120),
        GradientRotationSpeed = 1.2,
    },
}

local ScreenGui, MainFrame
local stealSpeedValue = CONFIG.StealSpeed.DefaultValue / 100
local triggerValue = CONFIG.Trigger.DefaultPercent / 100
local draggingSteal = false
local draggingTrigger = false
local activeTriggers = {}

-- ==================== UI FUNCTIONS ====================
local function createCorner(parent, radius)
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, radius or 6)
    c.Parent = parent
end

local function createStroke(parent, color, thickness)
    local s = Instance.new("UIStroke")
    s.Color = color or CONFIG.Accent.StrokeColor
    s.Thickness = thickness or 1.6
    s.Parent = parent
    return s
end

local function createGradient(parent)
    local g = Instance.new("UIGradient")
    g.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 25, 10)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(80, 255, 120)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 25, 10)),
    }
    g.Parent = parent
    return g
end

local function makeDraggable(frame)
    local dragging = false
    local dragInput, dragStart, startPos

    frame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = frame.Position
            dragInput = input
        end
    end)

    frame.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if dragging and input == dragInput then
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)

    frame.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = false
        end
    end)
end

-- ==================== GUI CREATION ====================
local function createGUI()
    ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = CONFIG.Gui.ScreenGuiName
    ScreenGui.ResetOnSpawn = CONFIG.Gui.ResetOnSpawn
    ScreenGui.ZIndexBehavior = CONFIG.Gui.ZIndexBehavior
    ScreenGui.Parent = CoreGui

    MainFrame = Instance.new("Frame")
    MainFrame.Name = CONFIG.MainFrame.Name
    MainFrame.Size = CONFIG.MainFrame.Size
    MainFrame.Position = CONFIG.MainFrame.Position
    MainFrame.BackgroundColor3 = CONFIG.MainFrame.BackgroundColor3
    MainFrame.BackgroundTransparency = CONFIG.MainFrame.BackgroundTransparency
    MainFrame.BorderSizePixel = CONFIG.MainFrame.BorderSizePixel
    MainFrame.Active = CONFIG.MainFrame.Active
    MainFrame.Parent = ScreenGui

    createCorner(MainFrame, 8)
    createGradient(createStroke(MainFrame, CONFIG.Accent.StrokeColor, 1.8))

    -- Title
    local TitleBar = Instance.new("Frame")
    TitleBar.Size = UDim2.new(1, 0, 0, CONFIG.TitleBar.Height)
    TitleBar.BackgroundTransparency = 1
    TitleBar.Parent = MainFrame

    local TitleLabel = Instance.new("TextLabel")
    TitleLabel.Size = UDim2.new(1, 0, 1, 0)
    TitleLabel.BackgroundTransparency = 1
    TitleLabel.Text = CONFIG.TitleBar.Text
    TitleLabel.TextColor3 = CONFIG.TitleBar.TextColor3
    TitleLabel.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
    TitleLabel.TextSize = CONFIG.TitleBar.TextSize
    TitleLabel.Parent = TitleBar

    -- Steal Speed Slider
    local function createSlider(yPos, config, onChange)
        local container = Instance.new("Frame")
        container.Size = UDim2.new(1, -20, 0, 42)
        container.Position = UDim2.new(0, 10, 0, yPos)
        container.BackgroundTransparency = 1
        container.Parent = MainFrame

        local label = Instance.new("TextLabel")
        label.Size = UDim2.new(1, -50, 0, 16)
        label.Position = UDim2.new(0, 0, 0, 0)
        label.BackgroundTransparency = 1
        label.Text = config.Label
        label.TextColor3 = Color3.fromRGB(180, 255, 200)
        label.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
        label.TextSize = 11
        label.TextXAlignment = Enum.TextXAlignment.Left
        label.Parent = container

        local valueLabel = Instance.new("TextLabel")
        valueLabel.Size = UDim2.new(0, 40, 0, 16)
        valueLabel.Position = UDim2.new(1, -40, 0, 0)
        valueLabel.BackgroundTransparency = 1
        valueLabel.Text = config.DefaultValue .. "%"
        valueLabel.TextColor3 = Color3.fromRGB(180, 255, 200)
        valueLabel.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold)
        valueLabel.TextSize = 11
        valueLabel.TextXAlignment = Enum.TextXAlignment.Right
        valueLabel.Parent = container

        local bar = Instance.new("Frame")
        bar.Size = UDim2.new(1, 0, 0, 5)
        bar.Position = UDim2.new(0, 0, 0, 24)
        bar.BackgroundColor3 = config.BarColor
        bar.Parent = container
        createCorner(bar, 4)

        local fill = Instance.new("Frame")
        fill.Size = UDim2.new(config.DefaultValue/100, 0, 1, 0)
        fill.BackgroundColor3 = config.FillColor
        fill.Parent = bar
        createCorner(fill, 4)

        local knob = Instance.new("Frame")
        knob.Size = UDim2.new(0, 12, 0, 12)
        knob.Position = UDim2.new(config.DefaultValue/100, -6, 0.5, -6)
        knob.BackgroundColor3 = config.KnobColor
        knob.Parent = bar
        createCorner(knob, 10)

        -- Slider Logic
        local function update(v)
            v = math.clamp(v, 0, 1)
            fill.Size = UDim2.new(v, 0, 1, 0)
            knob.Position = UDim2.new(v, -6, 0.5, -6)
            valueLabel.Text = math.floor(v * 100) .. "%"
            onChange(v)
        end

        bar.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 then
                draggingSteal = true -- veya draggingTrigger
                update((input.Position.X - bar.AbsolutePosition.X) / bar.AbsoluteSize.X)
            end
        end)

        UserInputService.InputChanged:Connect(function(input)
            if (draggingSteal or draggingTrigger) and input.UserInputType == Enum.UserInputType.MouseMovement then
                update((input.Position.X - bar.AbsolutePosition.X) / bar.AbsoluteSize.X)
            end
        end)

        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 then
                draggingSteal = false
                draggingTrigger = false
            end
        end)

        return update
    end

    local updateStealSpeed = createSlider(35, CONFIG.StealSpeed, function(v) stealSpeedValue = v end)
    local updateTrigger = createSlider(85, CONFIG.Trigger, function(v) triggerValue = v end)
end

-- ==================== FUNCTIONALITY ====================
local function setupFunctionality()
    makeDraggable(MainFrame)

    -- Gradient Animation
    local stroke = MainFrame:FindFirstChildWhichIsA("UIStroke")
    local grad = stroke and stroke:FindFirstChildWhichIsA("UIGradient")
    if grad then
        RunService.Heartbeat:Connect(function(dt)
            grad.Rotation = (grad.Rotation + CONFIG.Accent.GradientRotationSpeed * 60 * dt) % 360
        end)
    end

    -- Proximity Prompt Logic (Steal)
    ProximityPromptService.PromptButtonHoldBegan:Connect(function(prompt)
        if activeTriggers[prompt] then return end
        activeTriggers[prompt] = true

        local start = os.clock()
        local fired = false
        local conn

        conn = RunService.PreRender:Connect(function()
            if not prompt or not prompt.Parent then
                conn:Disconnect()
                activeTriggers[prompt] = nil
                return
            end

            if not fired and (os.clock() - start) / prompt.HoldDuration >= triggerValue then
                fired = true
                conn:Disconnect()
                activeTriggers[prompt] = nil

                local char = LocalPlayer.Character
                local tool = char and char:FindFirstChildOfClass("Tool")
                if tool then 
                    tool:Activate() 
                end
            end
        end)

        prompt.PromptButtonHoldEnded:Connect(function()
            if not fired then
                conn:Disconnect()
                activeTriggers[prompt] = nil
            end
        end)
    end)
end

createGUI()
setupFunctionality()
print("[Meowl Hub] Steal Script Loaded!")
